# Reports

This directory is for generated audit reports and visualization outputs.

Normal tests must not require local `/ssd` STED paths. Local reports are generated only when the real-data commands are run explicitly.

